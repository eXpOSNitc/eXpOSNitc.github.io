standard i/o thru read
links from expos to xsm
make a new page for special process (shell process)/* init process descrption */
idle process also..
keep the scheduler code ina box disk interrupt
change the disk free list data structure
complete forks add a different case for swapped stack pages
add data structures updated in fork and exec
reduce cell entry size of the table
complete exit(same prblm as fork)
green box in exit,exec and fork(swap portion)
give link for root entry in delete system call
add root file in disk data structure

